# Frontend (React + Vite)

Desde la carpeta `frontend/`:

1. Instalar dependencias:

```powershell
npm install
```

2. Iniciar en modo desarrollo:

```powershell
npm run dev
```

El dev server corre por defecto en `http://localhost:3000` y las llamadas a `/api` se proxyan a `http://localhost:5000`.
